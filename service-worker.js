// This service worker is included because the manifest requires it, 
// but it contains no functionality as all interaction is handled by the content script.
console.log('ClassCharts Notes Extension: Service Worker loaded.');